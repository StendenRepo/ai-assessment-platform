services:
    frontend:
        build:
            context: ..
            dockerfile: docker/frontend/Dockerfile
        ports:
            - '3000:3000'
        volumes:
            - ../frontend:/app
            - /app/node_modules
        environment:
            - CHOKIDAR_USEPOLLING=true
