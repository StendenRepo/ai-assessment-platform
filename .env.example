# Root .env for Docker Compose variable substitution.
# Copy this file to .env and fill in the values.

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ai_assessment

# Backend
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ai_assessment
UPLOAD_DIR=/app/data/uploads
RECORDING_DIR=/app/data/recordings
EXPORT_DIR=/app/data/exports

# Auth
JWT_SECRET_KEY=change-me-in-production-use-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=480
FRONTEND_URL=http://localhost:3000

# Ollama (default — Docker container in compose stack)
# Use a 7B+ model locally for plagiarism detection (pull once: ollama pull qwen2.5:7b)
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_MODEL_BACKUP=llama3.1:8b
OLLAMA_TIMEOUT_SECONDS=120

# Faster local dev on Mac (native Metal): ./scripts/use-host-ollama.sh
# sets OLLAMA_BASE_URL=http://host.docker.internal:11434 temporarily.
# Revert with: ./scripts/use-docker-ollama.sh

# Dedicated AI-text classifier (RoBERTa — runs in ai-detector container, not Ollama)
AI_DETECTOR_URL=http://ai-detector:9001
AI_DETECTOR_MODEL=Hello-SimpleAI/chatgpt-detector-roberta
AI_DETECTOR_TIMEOUT_SECONDS=120

# Seed admin account (used by the initial migration)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_NAME=Admin
SEED_ADMIN_PASSWORD=changeme